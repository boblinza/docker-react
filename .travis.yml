language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t boblinza/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true boblinza/docker-react npm run test 
# - docker run -e CI=true boblinza/docker-react npm run test -- --coverage